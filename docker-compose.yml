version: "3.3"

services:
  unit:
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: unit
    command:
      [
        "npm",
        "run",
        "start",
      ]
    restart: always
    volumes:
      - ./:/app
    ports:
      - 8085:8085
    networks:
      - 2023-2-capju-config_capju
    env_file:
      - .env

networks:
  2023-2-capju-config_capju:
    external: true
